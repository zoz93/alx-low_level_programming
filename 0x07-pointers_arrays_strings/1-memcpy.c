#include "main.h"
/**
 * _memcpy - a function copies memory area
 * @dest: memory destenatiom
 * @src: memory where is copied
 * n: number of bytes
 * Return: copied memory
 */
char *_memcpy(char *dest, char *scr, unsigned int n)
{
	int j = 0;
	int i = n;

	for (;r < i; r++)
	{
		dest[r] = scr[r];
		n--;
	}
	return (dest);
}
